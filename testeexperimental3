import { Body, Text } from 'native-base';
import React from 'react';
import { View, StyleSheet, Color,LogBox, SafeAreaView, FlexAlignType, flex, FlatList, Header, ScrollView} from 'react-native';


const App = () => {
  return (
    // Try removing the `flex: 1` on the parent View.
    // The parent will not have dimensions, so the children can't expand.
    // What if you add `height: 300` instead of `flex: 1`?
   

    <View style={styles.container}>

    <Text >Mesa:</Text>

      </View>
      
  );
};



const Bloco2 = () => {
  return (
    // Try removing the `flex: 1` on the parent View.
    // The parent will not have dimensions, so the children can't expand.
    // What if you add `height: 300` instead of `flex: 1`?
   
  
   <View style={styles.TeladeCima}> 
    <ScrollView>
    <Text >Nome:</Text> 
    <Text>Pedido : </Text>
    
    </ScrollView>
    </View>

  
      
  );
};






const styles = StyleSheet.create({
  container: {
    flex: 2,
    backgroundColor: 'orange',
 
    borderStyle: 'solid',
    borderColor: 'red',
    marginTop: 200,
  },

  TeladeCima: {
    flex: 1 , flexDirection:'row', 
    justifyContent:"center",
     
        width: '100%' , 
        height: 250,
         borderWidth: 4 , 
         borderStyle: 'solid' , 
         borderColor: 'orange' , 
         backgroundColor: 'blue',


  }


}); // tem q ter esses pontos aki



export default App;


